# Recommendarr — unified deployment
# Single container: FastAPI backend + React frontend
version: "3.8"

services:
  recommendarr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recommendarr
    restart: unless-stopped
    ports:
      - "${RECOMMENDARR_PORT:-5055}:5055"
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Europe/Zurich}
      - DATA_DIR=/app/data
    volumes:
      - ${DATA_DIR:-./data}:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5055/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    labels:
      - "net.unraid.docker.managed=dockerman"
      - "net.unraid.docker.webui=http://[IP]:[PORT:${RECOMMENDARR_PORT:-5055}]/"
